function test() {
  let message = "Welcome to Typescript";
  message.toLowerCase();

  //   message();

  const user = {
    name: "Daniel",
    age: 27,
  };

  //   user.location;
  const announce = "Welcome Folks";
  announce.toLocaleLowerCase();
  announce.toLocaleLowerCase();
}

function flip() {
  return Math.random() < 0.5;
}
